<div class="task" data-task-id={{_id}}>
  <div class="ui checkbox">
  {{#if completed }}
  	<input type="checkbox" name={{_id}} checked>
  {{else}}
  	<input type="checkbox" name={{_id}}>
  	<label>{{description}} {{dueDate}}</label>
  {{/if}}
  </div>
</div>
